#include "main.h"
#include <stdlib.h>
/**
 * _abs - ichecks if c is alphabete
 * @j: is variable to be checked
 * Return: returns the absoulute value of the input
*/
int _abs(int j)
{
	int ret;

	ret = abs(j);
	return (ret);
}
